name: Deploy WaterSensor on Environment

on:
  workflow_dispatch:
    inputs:
      serial_port:
        required: false
        type: string
      environment:
        required: true
        type: choice
        options:
          - Home

permissions:
  contents: read
  deployments: write

jobs:
  deployment:
    uses: Zefek/GithubActions/.github/workflows/arduino-build-deploy.yml@638a69ea83d67a640e5ec938f50a0e4489e7d979
    with:
      environment: ${{ inputs.environment }}
      serial_port: ${{ inputs.serial_port }}
      device_name: WaterSensor
      source_dir:  "./src/WaterSensor"
      ota:         true
    secrets: inherit
