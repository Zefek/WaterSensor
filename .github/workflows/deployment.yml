name: Deploy WaterSensor on Environment

on:
  workflow_dispatch:
    inputs:
      serial_port:
        required: false
        type: string
      environment:
        required: true
        type: choice
        options:
          - Home

permissions:
  contents: read
  deployments: write

jobs:
  deployment:
    uses: Zefek/GithubActions/.github/workflows/arduino-build-deploy.yml@8e0333bd7deb714e7950d84629ee855731e37672
    with:
      environment: ${{ inputs.environment }}
      serial_port: ${{ inputs.serial_port }}
      device_name: WaterSensor
      source_dir:  "./src/WaterSensor"
      ota:         true
    secrets: inherit
